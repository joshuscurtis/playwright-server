{
  "name": "playwright-report-server-reporter",
  "version": "0.1.0",
  "description": "Playwright reporter that uploads HTML reports to a self-hosted report server",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "peerDependencies": {
    "@playwright/test": ">=1.40.0"
  },
  "dependencies": {
    "archiver": "^7.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/archiver": "^6.0.0",
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^3.0.0"
  }
}
